import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Channel = () => {
  return (
    <View>
      <Text>Channel view</Text>
    </View>
  );
};

export default Channel;

const styles = StyleSheet.create({});
